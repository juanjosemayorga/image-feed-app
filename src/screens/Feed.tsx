import React from 'react'
import { ActivityIndicator, SafeAreaView, Text, View, ViewPropTypes } from 'react-native'
import { CardList } from '../components/CardList'
import { fetchImages } from '../utils/api'

export const Feed = () => {
  return (
    <Text>Feed</Text>
  )
}
